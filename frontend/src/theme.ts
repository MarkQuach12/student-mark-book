import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#ffa071",
      light: "#ffb694",
      dark: "#ff8a4f",
      contrastText: "#f0f0f0",
    },
  },
});

export default theme;
